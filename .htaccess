<IfModule rewrite_module>
    RewriteEngine on

# letsencrypt
	RewriteRule ^.well-known/ - [L,NC]

# statistics
	RewriteRule ^stats/ - [L,NC]
	
# 301 redirects to main domains
#	RewriteCond %{HTTP_HOST} !^www.domain.cz$ [NC]
#    #RewriteRule ^(.*)$ http://www.domain.cz/$1 [L,R=301]
#    RewriteRule ^(.*)$ https://www.domain.cz/$1 [L,R=301]
	
# rewrite to HTTPS:
#	RewriteCond %{HTTPS} off
#	RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
		
# everything else
 	RewriteRule	^(.*)$  www/$1 [L,QSA]
</IfModule>